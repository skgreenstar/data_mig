apiVersion: apps/v1
kind: Deployment
metadata:
  name: collector-dev
  namespace: strategicai
  labels:
    app: collector-dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: collector-dev
  template:
    metadata:
      labels:
        app: collector-dev
    spec:
      containers:
      - name: collector
        image: ghcr.io/greenstar/strategic-ai-collector:dev
        ports:
        - containerPort: 9000
        envFrom:
        - configMapRef:
            name: strategicai-dev-config
        - secretRef:
            name: strategicai-dev-secret
        volumeMounts:
        - name: data
          mountPath: /app/data_local
        readinessProbe:
          httpGet:
            path: /health
            port: 9000
          initialDelaySeconds: 20
          periodSeconds: 15
      volumes:
      - name: data
        persistentVolumeClaim:
          claimName: collector-data-dev-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: collector-dev
  namespace: strategicai
spec:
  type: NodePort
  selector:
    app: collector-dev
  ports:
  - protocol: TCP
    port: 9000
    targetPort: 9000
    nodePort: 30900
